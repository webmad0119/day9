var app = {
    init: function () {
        var canvasDOM = document.querySelector("#canvasExp")

        canvasDOM
            .setAttribute("width", window.innerWidth);

        canvasDOM
            .setAttribute("height", window.innerHeight)
    }
}